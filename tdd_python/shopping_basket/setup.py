from distutils.core import setup
from setuptools import find_packages

setup(
    name="shopping-basket",
    version="0.0.1",
    description="Test Driven Development of a simple shopping basket with price totalling"
)
